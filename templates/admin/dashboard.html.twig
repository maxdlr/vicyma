{% extends 'admin-base.html.twig' %}

{% block title %}
    {{ parent() }}
    Tableau de bord
{% endblock %}

{% block body %}
    {{ form_start(lodgingForm) }}
    {{ form_end(lodgingForm) }}

    {#    {{ dump(lodgings[0]) }} #}
{% endblock %}
