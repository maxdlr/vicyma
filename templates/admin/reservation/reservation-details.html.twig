{% extends 'admin-base.html.twig' %}

{% block title %}
    {{ parent() }}
    Tableau de bord
{% endblock %}

{% block body %}
    {{ include('_components/Organism/form/_form_reservation.html.twig', {
        save: true,
        delete: true,
        archive: true,
        confirm: true,
        setAsPaid: true,
        reference: 'Reservation de ' ~ reservation.user.firstname ~ ' ' ~ reservation.user.lastname
    }) }}
{% endblock %}